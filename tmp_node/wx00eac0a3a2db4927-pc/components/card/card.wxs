module.exports = ({
    getDay: (function(time) {
        if ((time + '').length === 10) {
            time = +time * 1000
        };
        var timestamp = getDate().getTime();
        return (parseInt(((time - timestamp) / (1000 * 60 * 60 * 24))))
    }),
});