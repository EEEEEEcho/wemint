module.exports = ({
    getProperty: (function(item) {
        if (typeof item.value === 'object') {
            return (item.value.value)
        } else {
            return (item.value)
        }
    }),
});