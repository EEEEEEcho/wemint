module.exports = ({
    getTime: (function(time) {
        if ((time + '').length === 10) {
            time = +time * 1000
        };
        var date = getDate(time);
        var y = date.getFullYear();
        var m = date.getMonth() + 1;
        m = m < 10 ? ('0' + m) : m;
        var d = date.getDate();
        d = d < 10 ? ('0' + d) : d;
        var h = date.getHours();
        h = h < 10 ? ('0' + h) : h;
        var minute = date.getMinutes();
        minute = minute < 10 ? ('0' + minute) : minute;
        return (y + '-' + m + '-' + d + ' ' + h + ':' + minute)
    }),
    getGoodNum: (function(good) {
        if (good == null) {
            return (0)
        };
        var numberValue = 0;
        for (var i = 0; i < good.length; i++) {
            numberValue = numberValue + good[((nt_0 = (i), null == nt_0 ? undefined : 'number' === typeof nt_0 ? nt_0 : "" + nt_0))].total
        };
        return (numberValue)
    }),
});