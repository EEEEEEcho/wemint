// \pages/update/update radioChange Data Flow
digraph {
	"FunctionCall,
function name -> radioChange,
param -> e." [label="FunctionCall,
function name -> radioChange,
param -> e." color=blue]
	"For Loop" [label="For Loop" color=blue]
	"Assignment,
left -> items[i].checked,
right -> items[i].worktype_name == e.detail.value" [label="Assignment,
left -> items[i].checked,
right -> items[i].worktype_name == e.detail.value" color=blue]
	"For Loop" -> "Assignment,
left -> items[i].checked,
right -> items[i].worktype_name == e.detail.value"
	"FunctionCall,
function name -> radioChange,
param -> e." -> "For Loop"
}
