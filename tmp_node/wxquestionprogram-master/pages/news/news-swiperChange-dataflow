// \pages/news/news swiperChange Data Flow
digraph {
	"FunctionCall,
function name -> swiperChange,
param -> e.detail" [label="FunctionCall,
function name -> swiperChange,
param -> e.detail" color=blue]
	"VariableDeclarator,
left -> source,
right -> e.detail" [label="VariableDeclarator,
left -> source,
right -> e.detail" color=blue]
	"FunctionCall,
function name -> swiperChange,
param -> e.detail" -> "VariableDeclarator,
left -> source,
right -> e.detail"
	"If Alternate" [label="If Alternate" color=blue]
	"CallExpression,
callee name -> this.setData,
param -> True" [label="CallExpression,
callee name -> this.setData,
param -> True" color=blue]
	"{
swiperCurrent -> current
}" [label="{
swiperCurrent -> current
}" color=blue]
	"CallExpression,
callee name -> this.setData,
param -> True" -> "{
swiperCurrent -> current
}"
	"If Alternate" -> "CallExpression,
callee name -> this.setData,
param -> True"
	"FunctionCall,
function name -> swiperChange,
param -> e.detail" -> "If Alternate"
}
